# Thêm domain alias vào danh sách dưới, ngăn cách bằng khoảng trắng
# Sau đó reload Caddy: caddy reload --config /etc/caddy/Caddyfile

webmail.bnix.asia,
webmail.example.com {
    reverse_proxy 127.0.0.1:8000
}
